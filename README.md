### Run PokeBinder Backend locally with Docker (development)

```bash
git clone https://github.com/daffa-pradana/pokebinder-backend
cd pokebinder-backend
docker compose up --build
# server: http://localhost:3000
```